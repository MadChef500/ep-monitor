[phases.install]
cmds = [
  "pip install --only-binary=:all: greenlet",
  "pip install -r requirements.txt"
]

[start]
cmd = "python -m playwright install --with-deps chromium && python scheduler.py"
